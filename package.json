{
    "scripts": {
        "dev": "cd src/dashboard && npm run dev",
        "build": "cd src/dashboard && npm run build",
        "generate": "cargo test generate_candid -p monitor && dfx generate monitor"
    }
}